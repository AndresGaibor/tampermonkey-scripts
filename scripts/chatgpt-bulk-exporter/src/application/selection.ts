export class SelectionStore { private selected = new Set<string>(); get size() { return this.selected.size; } get ids() { return [...this.selected]; } has(id: string) { return this.selected.has(id); } add(id: string) { this.selected.add(id); } remove(id: string) { this.selected.delete(id); } toggle(id: string) { this.selected.has(id) ? this.selected.delete(id) : this.selected.add(id); } clear() { this.selected.clear(); } }
